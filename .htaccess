<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /frgomes/

    # Se o arquivo ou diretório não existir, redireciona pra index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
