<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Redireciona tudo que não for arquivo/folder real pro index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>